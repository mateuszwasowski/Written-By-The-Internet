<div id = "h_background">
<header class="header">
  <li><%= link_to "WrittenByTheInternet", root_path%></li>
  <% if user_signed_in? %>
  <li><%= "#{current_user.first_name} #{current_user.last_name}" %> | <%= link_to "Sign Out", destroy_user_session_path, :method => :delete %> </li> 
  <% else %>
    <li><%= link_to "Log In", new_user_session_path %> | <%= link_to "Sign Up", new_user_registration_path %></li>
  <% end %>
</header>
</div>